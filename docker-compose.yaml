version: '3'

services:
  django_app:
    build: .
    container_name: django_app
    networks:
      - cicd_test_jenkins_network
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
    volumes:
      - .:/app

networks:
  cicd_test_jenkins_network:
    external: true
